function validFunc (password){
  // 2
//password.length > 7 ? console.log ("Strong") : console.log ("Weak");
// 3
  console.log((password.length > 7 && "strong"  )|| "week");
// 4
// password.length > 7 && password != password.toLowerCase() ? console.log("Very Strong") : password.length > 7 ? console.log ("Strong") : console.log ("Weak");

}